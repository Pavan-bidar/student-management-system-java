# student-management-system-java
The Student Management System is a simple desktop application developed using Java with Swing for the user interface and JDBC for database connectivity. The application uses MySQL as the backend database to store and manage student records.
